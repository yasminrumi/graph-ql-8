# GraphQL API Server

A complete GraphQL API implementation with CRUD operations for Users, Posts, and Products. Built with Express, GraphQL, and includes comprehensive automated tests using Node's built-in test module.

## Features

- ✅ Full CRUD operations for Users, Posts, and Products
- 📚 Complete GraphQL schema documentation
- 🧪 Automated tests using Node's built-in `node:test` module
- 🎨 GraphiQL interactive playground
- 🔄 RESTful operations converted to GraphQL queries and mutations
- 💾 In-memory data store (easily replaceable with database)

## Installation

```bash
npm install